import { styled } from "@mui/system";
import { Grid } from "@mui/material";

export const StyledClubsSection = styled("div")({
  padding: "50px 50px",
});

export const StyledGrid = styled(Grid)({
  textAlign: "center",
});
